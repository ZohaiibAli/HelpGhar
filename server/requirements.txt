fastapi
uvicorn
# Real-time messaging (routes/messaging_routes.py) speaks WebSocket.
# Bare uvicorn ships no WS protocol implementation, so without this the
# /messages/ws handshake is refused on a clean install.
websockets
pymongo
python-dotenv
email-validator
python-multipart
passlib[bcrypt]
bcrypt==4.0.1
python-jose[cryptography]
cloudinary

qdrant-client
langchain-text-splitters
google-genai

nltk==3.9.1
scikit-learn 
numpy
tzdata

fastapi-mail==1.6.5
